export const PRESCRIPTION_COLORS: Record<string, { color: string; bg: string }> = {
  Active: { color: "var(--accent-cyan)", bg: "var(--accent-cyan-subtle)" },
  Completed: { color: "var(--text-tertiary)", bg: "var(--bg-tertiary)" },
  Discontinued: { color: "var(--accent-red)", bg: "var(--accent-red-subtle)" },
};
